# @framesieve/cli

The fsieve command: replay framesieve recordings and sweep gate
parameters offline. Replays are pure functions of
(recording, options), so output is diffable across runs and machines.
See the main repository README: https://github.com/Jncch/framesieve
